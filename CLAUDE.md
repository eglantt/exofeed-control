# CLAUDE.md — ExoFeed Control

Этот файл загружается автоматически в начале каждой сессии Claude Code в этом репозитории. Он заменяет необходимость каждый раз объяснять контекст заново. **Этот файл выдаётся заново вместе с каждой новой сборкой** — актуализировать его нужно в том же ответе, где отдаётся обновлённый `index.html`, а не откладывать на потом.

## Что это за приложение

**ExoFeed Control** — учёт кормления, линек, инкубации кладок и содержания животных в террариумном хозяйстве (Алматы). Один самодостаточный HTML-файл (`index.html`, актуальная версия — см. `<title>` в файле) — весь HTML/CSS/JS в одном месте, без сборки, без внешних зависимостей (кроме опционального PWA service worker). Работает офлайн, вся база — в `localStorage` браузера. Развёрнуто на GitHub Pages, установлено как PWA на нескольких телефонах. Пользователи — 3-4 человека, работают по очереди, передают базу через ручной экспорт/импорт JSON.

## КРИТИЧЕСКИ ВАЖНО — непрерывность базы данных

Это правило нарушать нельзя ни при каких обстоятельствах — пользователь придаёт этому первостепенное значение, это не преувеличение, а буквальный факт живой рабочей базы:

1. У живой базы — версионный ключ `localStorage`: `exofeed_db_vX.Y.Z` (переменная `CURRENT_DB_KEY`). **При каждой новой версии, для любой правки, даже косметической:**
   - `CURRENT_DB_KEY` обновляется на новую версию;
   - **предыдущая** версия добавляется первой в массив `legacyKeys` (список всех обратно совместимых ключей для чтения при загрузке — код идёт по списку по порядку и берёт первый найденный).
   - Без этого шага база пользователя "слетит" при замене файла на GitHub Pages.
2. Есть отдельные, **не завязанные на версию** ключи (не требуют добавления в `legacyKeys`, читаются/пишутся напрямую по своему постоянному имени):
   - `exofeed_archive` (`ARCHIVE_KEY`) — архив выбывших животных;
   - `exofeed_archive_clutches` (`CLUTCH_ARCHIVE_KEY`, с v1.8.0) — архив завершённых кладок;
   - `exofeed_ui_collapsed_locs` (`UI_COLLAPSE_KEY`) — свёрнутые локации в интерфейсе;
   - `exofeed_engineer_pin` (`ENGINEER_PIN_KEY`) — PIN инженерного режима;
   - `exofeed_custom_species` (`CUSTOM_SPECIES_KEY`) — пользовательский список видов;
   - `exofeed_db_modified_at` (`DB_MODIFIED_KEY`) — метка времени последнего изменения (используется при импорте для сравнения версий);
   - `exofeed_incubation_reminders_shown` (`INCUBATION_REMINDER_KEY`, с v1.8.2) — какой кладке в какой день уже показали напоминание (дедупликация "раз в сутки").
   - `exofeed_pending_batches` (`PENDING_BATCH_KEY`, с v1.8.7) — активные пакеты отмены массовых действий (5-минутное окно). Специально НЕ включён в бэкап/импорт — короткоживущий, привязан к конкретному устройству/моменту, восстанавливать его из старого бэкапа было бы бессмысленно и путано.
3. Миграция старых структур данных — в функциях `normalizeCell()`, `normalizeAnimal()`, `normalizeClutch()` (с v1.8.0), `migrateBlock()`. Каждое новое поле добавляется туда с `if (x === undefined) x = ...` — старые записи без этого поля получают безопасное значение по умолчанию. Если поле не просто добавляется, а **меняет форму** (пример: `clutch.layDate` (дата) → `clutch.layDateTime` (дата+время) в v1.8.2) — старое поле конвертируется, а затем **удаляется** (`delete clutch.layDate`), не остаётся мёртвым дублем.
4. **Каждая новая версия обязана** пройти тест "старая база версии N-1 корректно читается версией N" — это не опционально. По возможности — тест именно с той версией, что реально задеплоена на GitHub Pages на момент правки (не абстрактной "предыдущей"), т.к. это единственный сценарий миграции, который случится в реальности.
5. `<title>`, `.version-tag`, `CURRENT_DB_KEY`, `legacyKeys`, `version` в объекте бэкапа (`downloadBackupFile`) обновляются **всегда**, для любой версии — даже чисто визуальной/косметической правки (CSS, размер поля и т.п.). Расхождение версии в `<title>` и в `CURRENT_DB_KEY` не допускается.

## Модель данных (актуальная на v1.8.15)

```
db = [ location {
  id, name,
  ambientTempLog: [{timestamp, value}],  // v1.8.6: фоновая температура ПОМЕЩЕНИЯ целиком, не привязана
                        // к стеллажу/ячейке — отдельный журнал, своя кнопка в шапке локации
  blocks: [ block (стеллаж) {
    id, name,
    type,              // строка-ЛЕЙБЛ для отображения ([Террариум]/[Вольер]/... в шапке блока), НЕ enum,
                        // определяющий поведение кода — ЗА ИСКЛЮЧЕНИЕМ значения 'Инкубатор' (см. ниже),
                        // которое единственное реально влияет на логику через INCUBATOR_BLOCK_TYPE
    targetTempLog: [{timestamp, tempMin, tempMax}],  // v1.8.2: целевой РЕЖИМ (не наблюдение) блока-инкубатора,
                        // с гистерезисом; актуальный режим = последний элемент массива, история хранится вся
    rows: [ row (полка) {
      cells: [ cell {
        label, isService, width, tempLog: [{timestamp, value}],
        animals: [ animal {
          species, gender, age, birthMonth, birthYear,
          notesLog: [{timestamp, text}],
          feed_logs: [{
            timestamp,        // момент, когда КО был ПРЕДЛОЖЕН — НЕ перезаписывается при разрешении "на ночь" (v1.6.0)
            date, type, sizeNum, sizeTitle, weightUnit, count,
            eatenCount,       // null пока не разрешено ("оставлен на ночь"), иначе число
            diedCount,        // v1.8.9: сколько из НЕ съеденного погибло (только для koState==='live'; для
                              // предубит/разморозка всегда 0 — понятие "погиб" неприменимо к уже не живому)
            status,           // 'Съедено' | 'Отказ' | 'Частично' | 'Оставлен на ночь' | 'Срыгивание'
            refusalReason, comment, forced,
            koState,          // v1.6.0: 'prekilled' | 'thawed' | 'live' — состояние КО на момент подачи
            utilized,         // v1.6.0: true/false/null(ещё не разрешено) — идёт ли КО в расход (см. ниже)
            resolvedAt        // v1.6.0: момент утренней фиксации результата (не совпадает с timestamp)
          }],
          shed_logs: [{date, status, timestamp}],
          shed_status
        } ],
        clutches: [ clutch {                          // v1.8.0: отдельная сущность-"жилец" ячейки, НЕ animal;
          id, layDateTime,                             // дата+время закладки (v1.8.2; было только layDate до v1.8.2)
          eggCount, hatchedCount,
          status,            // 'incubating' | 'partially_hatched' | 'hatched' | 'died'
          species,           // v1.8.2: вид животного, с автоподсказкой (datalist) из видов уже в базе животных
          estimatedDays,     // v1.8.2: примерный срок инкубации, суток (число или null)
          parentText,        // текстом, если без привязки к конкретной особи
          parentRefs: [{species, gender, age, locName, blockName, address, snapshotAt}], // СНИМОК на момент
                             // привязки, НЕ живая ссылка — в базе животных нет стабильных ID (адресация позицией)
          paramLog: [{timestamp, temp, humidity}],     // журнал НАБЛЮДЕНИЙ кладки, отдельный от tempLog ячейки
                             // И от targetTempLog блока — это фактические измерения, а не заданный режим
          createdAt
        } ]
      } ]
    } ]
  } ]
} ]

archiveDb        = [{ id, animal, reason, reasonText, archivedAt, originLocName, originBlockName, originAddress }]
clutchArchiveDb  = [{ id, clutch, reason, archivedAt, originLocName, originBlockName, originAddress }]  // v1.8.0
pendingBatches   = [{ id, type: 'feed'|'check', locId, blockId, label, createdAt, expiresAt,            // v1.8.7
                       items: [{cellIdx, animalIdx, logIdx, isNew, prevSnapshot, touched}] }]           // короткоживущее, НЕ в бэкапе
pendingActions   = [{ id, kind: 'delete_cell'|'delete_block'|'delete_location'|'archive_animal'|        // v1.8.10
                       'archive_clutch'|'transfer_animal', locId, blockId, label, createdAt, expiresAt,
                       revert: {...} }]  // форма revert разная под каждый kind, см. ниже; тоже НЕ в бэкапе
```

Адрес ячейки (`A1`, `B3`...) **не хранится** — вычисляется на лету по позиции в массивах (`rowLetter()`, `getCellAddress()`, `locateCellPosition()`).

### Логика утилизации КО (v1.6.0, доработана в v1.8.3/v1.8.4/v1.8.9) — "Расход КО"

- Предубит/разморозка → утилизирован (`utilized: true`) **всегда**, независимо от исхода, `diedCount` для них всегда 0 (понятие "погиб" неприменимо — уже не живой). Массовая кнопка «Не съедено» в утренней проверке (v1.8.3) применяет это же правило пакетно.
- Живой + съеден (полностью/частично) → утилизирован в съеденной части всегда.
- Живой + не тронут полностью или частично → **с v1.8.9** не бинарный вопрос "жив/погиб" на весь остаток, а числовое поле `diedCount` — сколько из непойманного остатка реально погибло за ночь (остальное, `count - eatenCount - diedCount`, посчитано как выжившее и не идёт в расход, можно переиспользовать для другого кормления обычной новой записью). `utilized = (eatenCount + diedCount) > 0`. Поле показывается в индивидуальной карточке при ЛЮБОМ непойманном остатке (`eaten < count`), не только при `eaten === 0` — раньше вопрос "жив/погиб" был виден только при полном отказе, что не позволяло отразить "съедено 1 из 3, из оставшихся 2 один погиб".
  **Массовая утренняя проверка (кнопка для живого КО, v1.8.4) сознательно НЕ детализирована** — там по-прежнему бинарный переключатель "Жив"/"Погиб" на весь выбор (пользователь явно решил не усложнять массовое действие). `resolveNightFeedCore` поддерживает оба вызова: новый параметр `died` (число, из индивидуальной карточки/формы правки) и старый `aliveAfter` (булево, из массовых кнопок) — если `died` не передан, он выводится из `aliveAfter` (`false` → весь остаток погиб, `true` → никто), это в точности воспроизводит прежнее поведение массовых кнопок без изменений.
- Срыгивание → утилизирован всегда.
- В отчётах колонка называется **"Расход КО"** (не "Утилизация" — переименовано в v1.7.0). С v1.8.9 текст колонки для живого КО с частичной гибелью показывает точное распределение (`formatUtilizedText_()`, например "Съедено 1, погибло 2, живо 1") вместо грубого "Да"/"Нет"; расчёт суммы расхода в `aggregateFeedingData` — `eaten + diedCount` для живого КО, `count`/`0` по `utilized` для остальных (не изменилось).
- **Миграция старых записей (до v1.8.9):** если запись была отмечена старым бинарным "Погиб" (`koState==='live', utilized===true, eatenCount===0`, уже разрешённая) — `diedCount` при загрузке выставляется равным всему непойманному остатку (`count - eatenCount`), чтобы суммы расхода в уже накопленных отчётах не "уменьшились" задним числом после обновления версии.

### Инкубация (v1.8.0–v1.8.2) — ключевые архитектурные решения

- Блок-инкубатор — это `block.type === 'Инкубатор'` (константа `INCUBATOR_BLOCK_TYPE`), та же структура `rows`/`cells`, что у обычных стеллажей. Единственный тип блока, где `type` реально ветвит поведение кода (везде ещё это просто лейбл).
- Ячейки инкубатора вмещают **только кладки** — животных туда нельзя переселить/скопировать/восстановить из архива (фильтр `b.type !== INCUBATOR_BLOCK_TYPE` в `renderTransferBlocks`/`renderCopyBlocks`/`renderRestoreBlocks`). В одной ячейке может быть несколько кладок одновременно — это нормальный, а не крайний случай.
- `block.targetTempLog` (режим, с гистерезисом min/max) и `clutch.paramLog` (фактические наблюдения температуры+влажности) — два принципиально разных журнала, не путать при доработках. Первый нужен "для сверки", второй — фактические измерения.
- Вылупившиеся особи заводятся **вручную** через обычную форму добавления животного — никакой автосвязи между кладкой и новой записью `animal` нет, это осознанное решение.
- Привязка родителя к кладке — **снимок** данных на момент выбора (`parentRefs`), не живая ссылка, потому что у животных в базе нет стабильных ID (адресация только позицией в массивах). Если после привязки животное переедет/будет отредактировано — в кладке останется то, что было на момент привязки.
- Визуальное выделение инкубатора — бирюзовый (`--incubator: #00897b`), выбран специально НЕ из уже занятых цветов (`--blue` = обычная занятая ячейка, `--green` = вылупилась/успех, `--red` = погибла/удаление/опасность повсюду в приложении, `--gold` = инженерный режим + частично вылупилась, `--purple` = массовое кормление). При любой новой цветовой кодировке — сверяться с этой таблицей значений, не занимать чужой смысл.
- Напоминания об инкубации (v1.8.2) — `collectIncubationReminders()` считает кандидатов (статус активный, срок указан, `daysElapsed >= estimatedDays - 7`), `startIncubationReminders()` запускается через `setTimeout(..., 5000)` после первого `render()`, показ каждого баннера 10 сек / закрытие тапом, между несколькими в очереди — пауза 3 сек. Дедупликация "раз в сутки на кладку" — через `reminderShownMap` в `exofeed_incubation_reminders_shown` (не версионный ключ).

## Ключевые архитектурные решения (не переизобретать, если попросят похожее)

- **PIN на инженерный режим** — защита только от случайного нажатия, не от целенаправленного обхода (статичный клиентский файл, пользователь честно предупреждён). Структурные/деструктивные действия — под `class="edit-ui"`, видны только в инженерном режиме.
- **Календарь и подсветка по дате** — `calendarPreview`, временно перекрашивает реальный стеллаж, не дублирует схему.
- **Массовое кормление** (`massFeedState`) — ограничено ОДНИМ стеллажом одновременно, осознанно.
- **Массовая утренняя проверка** (`morningCheckState`) — тоже один стеллаж, но НЕ через выбор ячеек галочками (это разные по природе задачи): список уже известных записей "оставлено на ночь" собирается сам, у каждой строки — чекбокс. Три кнопки массового действия (v1.8.3/v1.8.4): "Съедено полностью" и "Не съедено" — только для предубитого/размороженного КО; "Применить выбранное" для живого КО — по индивидуальному переключателю "Жив"/"Погиб" у каждой строки. Смешивать эти группы в одной кнопке нельзя — живой и не-живой КО подчиняются разной логике утилизации (см. выше).
- **Открытие ячейки** — нативное событие `click`, не ручное отслеживание жеста. Кастомный `pointerdown/move/up` — только для drag-and-drop в инженерном режиме (`attachCellDragHandlers`).
- **Перестановка локаций/стеллажей** — кнопки ▲/▼, сознательно НЕ drag-and-drop.
- **Крестик закрытия модалок** — инжектится `initModalCloseButtons()` во все `.modal` при старте.
- **Отчёты** — генерируются как отдельный самодостаточный HTML через `wrapReportDocument()`. `showReportModal(title, bodyHtml, filename, downloadBodyHtml?)` — необязательный 4-й параметр для отчётов с интерактивными элементами (выпадающие списки, кнопки копирования), которые не имеют смысла в статичном скачиваемом файле; если не передан — используется `bodyHtml` для обоих случаев.
- **Общая логика подсчёта отчётов** — `aggregateFeedingData(loc, startMs, endMs)` используется и отчётом за скользящую неделю (`openWeeklyReport`, `endMs = Infinity`), и отчётом за календарную неделю (`openCalendarWeekReport`, границы понедельник–воскресенье). Рендер вынесен в `renderTypeSummaryTable`/`renderSpeciesSummaryTable`/`renderChronoTable` — переиспользуются, чтобы правки не расходились между отчётами. Отчёт по инкубации (`openIncubationReport`, v1.8.0) — по тому же паттерну `showReportModal`.
- **Карта расселения** (v1.8.11, доработка вида в v1.8.12) — `buildResidentMapLocationHtml(loc)` строит HTML для ОДНОЙ локации (карта по стеллажам/рядам + список животных + объект `stats`), переиспользуется и одиночным входом (`openResidentMapReport(locId)` — из "Статистика и отчёты" конкретной локации), и общим (`openResidentMapReportAll()` — кнопка в нижней навигации, просто вызывает строитель в цикле по всем локациям и добавляет сводную таблицу сверху) — тот же принцип общего строителя, что и у `aggregateFeedingData`. Вёрстка карты — **не HTML-таблица с фиксированными столбцами**, а `display:flex; flex-wrap:wrap` на уровне ряда (класс `.res-map-cells`), чтобы длинные ряды (например, 20 террариумов подряд на реальной экспозиции) сами переносились по ширине печатной страницы, как перенос слов в тексте, без обрезания и без сжатия до нечитаемости; перенос происходит только ВНУТРИ одного ряда — каждый ряд свой подписанный блок (`.res-map-row`), ряды никогда не мешаются между собой. Блоки-инкубаторы исключены из карты (`block.type !== INCUBATOR_BLOCK_TYPE`) — отчёт про расселение животных, не про кладки.
  **Плитка ячейки (`.res-map-cell`, переработана в v1.8.12)** — сознательно НЕ повторяет `.cell` из основного приложения буквально: та же фиксированная ширина и цветовая логика рамки (синяя `occupied` = занято, штриховка `service` = служебное), но **высота растёт под содержимое, текст никогда не обрезается** — у настоящих `.cell` на экране текст обрезается до 2 строк (`-webkit-line-clamp: 2`), в отчёте это неприемлемо (нужно видеть ВСЁ содержимое для реального использования на бумаге). Если при доработке карты возникнет соблазн скопировать стиль `.cell` один в один — не делать этого, обрезание текста здесь намеренно исключено.
  Печатные CSS-классы (`.res-map-*`, `.loc-section` с `page-break-before`) продублированы в двух местах, как и `.rep-table` до этого: тёмная версия в основном `<style>` приложения (для превью внутри `modal-report`, визуально максимально похожа на экран приложения — именно это и было целью правки в v1.8.12) и светлая версия внутри `wrapReportDocument()` (для скачиваемого/печатного файла — фон сознательно светлый, а не тёмный, как на экране, чтобы не тратить чернила/тонер при печати; цветовая ЛОГИКА рамок при этом та же) — при правке стилей карты трогать оба места, иначе превью и итоговый файл разъедутся по виду. `.res-map-cells` использует `align-items: stretch` (не `flex-start`, поправлено в v1.8.13) — иначе пустые ячейки визуально "ужимаются" по высоте рядом с занятыми многострочными соседями по той же визуальной строке ряда.
- **Потребность в КО / Статистика поедаемости** (v1.8.14, упрощены и переименованы в v1.8.15 по прямой правке пользователя) — два самостоятельных отчёта. **Разные метрики, не путать**: «Потребность в КО» (`aggregateOfferedKO`) считает **предложено** (`log.count`) за календарный диапазон дат, **включая ещё не разрешённые "Оставлен на ночь"** записи — это НЕ то же самое, что `aggregateFeedingData()` (которая считает съедено/расход и такие записи из сводки исключает); переиспользовать `aggregateFeedingData` для этого отчёта было бы неверно, понадобился отдельный агрегатор. «Статистика поедаемости» (`aggregateEatingStats`, изначально называлась «Типичная порция») вообще не привязана к календарным датам — для КАЖДОГО животного берутся его последние (до) `n=4` `feed_logs` по факту (`sort` по `timestamp` descending, `slice(0, n)`), и среднее считается делением суммы на количество РЕАЛЬНО найденных записей (может быть меньше 4, если у животного мало записей).
  **Разная детализация у двух отчётов — сознательное решение, не унификация "на всякий случай":** у «Потребности в КО» **НЕТ** разбивки ни по видам животных, ни по локациям (в варианте "вся коллекция") — только ОДНА таблица тип+размер КО (первая версия v1.8.14 такую разбивку имела и формулировки вроде "для закупки", пользователь явно попросил убрать — если понадобится похожий отчёт снова, не возвращать разбивку по умолчанию, сначала спросить). У «Статистики поедаемости», наоборот, разбивка по видам животных **сохранена** — там она содержательно уместна (сама суть отчёта — "как питается каждый вид"). Если в будущем добавляется третий подобный отчёт — обязательно уточнять желаемую детализацию заново, не копировать по аналогии с одним из этих двух.
  Окно "Потребности в КО" — не "последние 28 дней от сегодня", а 28 дней **назад от даты, которую выбирает пользователь** в `modal-ko-need-date` (по умолчанию сегодня) — так отчёт остаётся полезным, даже если приложением давно не пользовались и нужна статистика за прошлый период; дата в шапке отчёта показывается явно (`formatDateDMY_`).
- **Поля заметок/причин/комментариев** (v1.8.5) — все текстовые поля этого рода (комментарий и причина при ночном разрешении/утренней проверке, родитель/комментарий у кладки, своя причина архивации, инлайн-правка комментария в истории лога) — многострочные `<textarea>`, не однострочный `<input type="text">`. Общий CSS-стиль (тёмная тема) висит на селекторе `input, select, textarea` — если добавляется новое текстовое поле такого рода, использовать `<textarea rows="2">`, а не `<input type="text">`, для единообразия. При рендере значения из данных внутрь `<textarea>...</textarea>` (не через атрибут `value`) — обязательно эскейпить через `escapeHtml()` (экранирует `& < >`; эскейпить только кавычки, как раньше делалось для атрибута `value`, недостаточно).
- **Отображение даты+значения в температурных журналах** (v1.8.6) — единый формат `${formatLogDateTime(ts)}: <b>значение</b>` (двоеточие), НЕ длинное тире (`—`, было так до v1.8.6). Касается трёх существующих журналов (`cell.tempLog`, `clutch.paramLog`, `block.targetTempLog`) и нового `loc.ambientTempLog` — если добавляется ещё один температурный/показательный журнал, использовать тот же формат для консистентности. История кормления (`feed_logs`) этот формат НЕ использует (там `—` перед описанием корма, не значением) — сознательно не трогалось, разные по смыслу записи.
- **Терминология "фоновая температура"** — с v1.8.6 это специфический термин для `loc.ambientTempLog` (температура помещения целиком). Формулировка в модалке показания ячейки (`modal-add-temp`) намеренно НЕ использует слово "фоновая" (было так исторически, поправлено в v1.8.6 на "Температура в этой ячейке...") — чтобы не путать два разных по смыслу журнала при дальнейших правках.
- **Отмена массовых действий** (v1.8.7, `pendingBatches`) — только для массового кормления и 3 кнопок массовой утренней проверки, НЕ для индивидуальных действий (сознательное решение пользователя). Записи адресуются по позиции (`cellIdx`/`animalIdx`/`logIdx`), как и везде в приложении — тот же класс риска при структурных правках во время активного окна (см. предупреждение в истории версий). Каждое массовое действие — свой независимый пакет со своим таймером, несколько пакетов могут существовать одновременно (сознательный выбор пользователя, не "последний заменяет предыдущий"). Данные пишутся в базу СРАЗУ (не черновик/два состояния) — "отмена" технически означает откат к снятому заранее снимку (`prevSnapshot`, для изменённых записей утренней проверки) или удаление только что созданной записи (`isNew: true`, для кормления). Запись, поправленная вручную через обычное редактирование (`markLogTouched`), исключается из отката именно этого пакета, но сам пакет остаётся активным для остальных записей. Обратный отсчёт обновляется тем же `setInterval`, что тикает часы в шапке (`tickClock` → `tickPendingBatchCountdowns`) — без полной перерисовки каждую секунду, только текст бейджа; полная перерисовка — только когда пакет реально истёк.
- **Отмена критических индивидуальных действий** (v1.8.10, `pendingActions`) — параллельный `pendingBatches` механизм с той же идеей (снимок "до" + 5 минут + переживает перезапуск + тот же `tickPendingBatchCountdowns`/`formatBatchCountdown`), но НЕ втиснут в структуру "список записей кормления" — у каждого `kind` своя форма `revert`, потому что снимки принципиально разные (целая ячейка со всем содержимым vs целый стеллаж vs целая локация vs одно животное vs одна кладка vs адреса источника/цели переезда). Покрывает: `delete_cell`, `delete_block`, `delete_location`, `archive_animal`, `archive_clutch`, `transfer_animal`. **Два разных места отображения** — принципиальное архитектурное решение: `delete_cell`/`archive_animal`/`archive_clutch`/`transfer_animal` показываются в шапке того стеллажа, где произошли (`getActivePendingActionsForBlock`, стеллаж ещё существует, ровно тот же `.pending-batch-badge` в render()); `delete_block`/`delete_location` показываются в отдельном **глобальном** контейнере `#global-pending-actions` (`renderGlobalPendingActions`, вызывается в начале каждого `render()`), потому что у них нет живого "родителя" для встраивания бейджа — сам стеллаж/локация уже удалены. При добавлении новых видов критических действий в `pendingActions` — сразу решать, к какой из этих двух групп они относятся (есть ли ещё живой стеллаж-контейнер на момент показа бейджа), а не считать, что блочная шапка подходит всегда. Откат — по индексу в `pendingActions` (`pendingActions.indexOf(...)`, как и у `pendingBatches`), не по `id` — та же причина (не встраивать float-числа как JS-литерал в `onclick`, см. заметку про архивные записи выше). Тот же принятый риск позиционной адресации, что и у переезда животного (v1.8.8) и у отдельного плана "устойчивые ID везде" в разделе "Отложенные направления" ниже — `revertPendingAction` оборачивает откат в `try/catch` и честно предупреждает пользователя, если структура успела поменяться настолько, что откат не удался, а не падает молча.
- **Порядок операций при перемещении/удалении данных между сущностями** (закреплено в v1.8.8 после реального бага с переездом животного) — правило на будущее для ЛЮБОЙ функции, которая одновременно удаляет что-то из одного места и добавляет в другое (трансфер животного — не единственный такой случай, тот же класс риска в принципе может быть где угодно, где есть `splice`+`push` в паре): **сначала проверить, что цель существует и валидна, и только потом удалять из источника**. Ни в коем случае не наоборот — если между удалением и добавлением есть точка, где что-то может пойти не так (пустой список, `undefined`, исключение), удаление, случившееся раньше проверки, необратимо и не отменяется автоматически (нет отдельного отката для одиночных действий — `pendingBatches` покрывает только массовые). При добавлении новых подобных операций — сверяться с этим правилом заранее, а не находить постфактум через баг-репорт, как было с трансфером.
- Русские склонения — `pluralizeRu(n, one, few, many)`.

## Порядок работы

1. **Крупные/неоднозначные фичи** — сначала план, уточняющие вопросы (не больше одного-трёх за раз). **Не писать и не редактировать код НИ ПРИ КАКИХ ОБСТОЯТЕЛЬСТВАХ, пока пользователь явно не подтвердил план И не дал отдельную явную команду ("пиши код" / аналогичную)** — это касается любых правок, включая мелкие однострочные фиксы и косметику, не только крупных фич. Обсуждение плана и его реализация — всегда два раздельных шага, даже если правка кажется тривиальной.
2. **Простые точечные правки** — план на 2-3 предложения без уточняющих вопросов, если и так понятно что делать, но команда "пиши код" всё равно нужна перед правкой файла.
3. **Каждая новая версия**: `<title>`, `.version-tag`, `CURRENT_DB_KEY`, `legacyKeys`, `version` в объекте бэкапа, версия (и краткий changelog) в `README.md` — всегда, для любой правки, включая чисто визуальную.
4. Пользователь заливает `index.html` на GitHub Pages (или это происходит через merge в `main`, если настроен деплой из ветки) — каждая версия должна пережить это обновление без потери данных. Если пользователь подтверждает, что уже успел залить предыдущую версию до того, как найден баг в ней — новая версия мигрирует именно с той (уже задеплоенной) версии, а не с абстрактной "предыдущей по счёту".
5. **Этот файл (`CLAUDE.md`) актуализируется вместе с каждой сборкой** — версия, модель данных, список архитектурных решений и история версий должны отражать реальное текущее состояние `index.html`, а не отставать от него.

## Тестирование (обязательный ритуал перед сдачей версии)

`bash` + Node.js + `jsdom` (`npm install jsdom`). После **каждого** значимого блока правок:

1. Извлечь `<script>` блоки, прогнать `node --check`.
2. jsdom-тесты через реальные DOM-события (`dispatchEvent(new win.MouseEvent('click', {bubbles:true}))`), не прямые вызовы функций — так ловятся баги в проводке событий. Исключение — намеренное: заведение вспомогательных данных для сценария теста (например, серия кормлений через `applyFeedingRecord(...)` напрямую, чтобы не писать десяток кликов ради данных, которые сами по себе не являются предметом теста) — это нормально, если сам новый функционал всё равно проверяется кликами.
3. Тест миграции: данные ПРЕДЫДУЩЕЙ версии (а если известно, что именно она уже задеплоена — то именно её) в `localStorage`, прогнать загрузку — сверить поле-в-поле, включая случаи, где поле не просто добавляется, а меняет форму (см. пример `layDate` → `layDateTime` выше).
4. Полный регресс по учётным путям, которые могла задеть правка: создание локации/стеллажа/животного, drag-and-drop, календарь, архив (животных и кладок), поиск, импорт бэкапа. Не обязательно гонять весь регресс целиком при каждой мелкой правке — но явно проверять то, что могло сломаться (пример: правка CSS навигации не требует теста миграции данных, а правка `normalizeClutch()` требует).
5. Тестовые скрипты хранятся в репозитории: `tests/test_v1_8_0.js` — **несмотря на имя файла, это живой, непрерывно дополняемый набор тестов, покрывающий версии вплоть до текущей (сейчас v1.8.15)**, а не тесты только для v1.8.0. Файл не переименовывался, чтобы не терять историю правок — при доработке новой версии дополнять именно его, а не создавать `test_v1.8.16.js` и т.п. При правках, затрагивающих общую логику, явно проверять, что старые тесты на предыдущие версии всё ещё проходят (весь файл прогоняется целиком — это дёшево, там больше 225 проверок).

### Особенности jsdom, уже отловленные (не наступать на те же грабли)

- `new JSDOM(...)` обязательно с `url: 'http://localhost/'`, иначе `localStorage` кидает `SecurityError`.
- `element.innerText` в jsdom **не работает** ни на чтение, ни на запись (тихо ничего не делает) — используй `.textContent`. В коде приложения `showReportModal` использует `innerText` для заголовка — это осознанно не трогалось, при тестировании заголовка проверяй что-то другое (например, `window.currentHtmlReportFilename`), а не `textContent` этого элемента.
- В файле есть `document.write()` (генерация опций года рождения), рассчитанный на исполнение ВО ВРЕМЯ парсинга документа (как в браузере). Если нужно засеять `localStorage` до того, как отработает код инициализации базы — используй `beforeParse(win) { ... }` в опциях `new JSDOM(html, {...})` с `runScripts: 'dangerously'`, а НЕ `runScripts: 'outside-only'` + ручной `eval` скриптов после полной загрузки — это стирает весь документ повторным `document.write`.
- Верхнеуровневые `let`/`const` в `<script>` (`db`, `archiveDb`, `clutchArchiveDb`, `isLocked`, `CURRENT_DB_KEY`, `state`...) — **не** становятся свойствами `window`, в отличие от объявлений `function` (те, включая все функции инкубации/утренней проверки/напоминаний, вызываются напрямую как `win.someFunction()`). Читать/писать переменные можно только через `win.eval('varName')` / `win.eval('varName = ...')`. Рабочий паттерн для записи сложных объектов: `win['__tmp_x'] = value; win.eval('x = window.__tmp_x;')`.
- `URL.createObjectURL` не реализован — стабить вручную (`win.URL.createObjectURL = () => 'blob:mock'`).
- Для pointer-based drag-тестов — стабить `el.setPointerCapture = () => {}`.
- `setInterval` (тикающие часы) держит процесс Node живым — обязательно `process.exit(0)` в конце тестового скрипта (либо явный код возврата через `process.exit(failures === 0 ? 0 : 1)` после подсчёта провалов).
- Реальные `setTimeout`-задержки (напоминания об инкубации: 5 сек/10 сек/3 сек) в тестах **не выжидаются** — тестируются напрямую вызовом функций (`collectIncubationReminders()`, `startIncubationReminders()`, `hideCurrentReminder()`), а не через реальное ожидание таймеров. Это законная, а не урезанная проверка: сама бизнес-логика (кого включать в напоминания, дедупликация, текст) проверяется полностью, просто без траты реального времени на паузы, которые к этой логике отношения не имеют.

## Файлы репозитория

- `index.html` — основное приложение.
- `manifest.json`, `sw.js`, `icons/*.png` — PWA-обвязка. `sw.js` использует network-first для `index.html` — CACHE_NAME не обязательно поднимать при каждой версии приложения, только при смене состава файлов PWA-обвязки или осознанном желании принудительно обновить офлайн-копию.
- `README.md` — инструкция по деплою + changelog по версиям в шапке, обновляется при каждом релизе.
- `tests/test_v1_8_0.js` — единый, непрерывно дополняемый тестовый файл (см. раздел "Тестирование" про имя файла).
- `SYNC_PLAN.md` — план и инструкции по синхронизации (Google-таблица + Apps Script) и переносу в новый репозиторий; актуализировать вместе с `CLAUDE.md`, когда работа по этой теме продвигается.
- `CLAUDE.md` — этот файл.

## Стиль общения

Пользователь — технически подкованный, но не программист; пишет по-русски, ценит точность и честность (в том числе когда я нахожу свои собственные баги — говорить прямо, без самобичевания). Объяснения — по существу, с указанием точной причины/строки, если это баг. Транслировать уверенность там, где она обоснована тестами, и явно помечать гипотезы/предположения, когда прямого подтверждения нет. Пользователь регулярно уточняет и сужает мои же первые формулировки вопросов ("да, но..." / "нет, вообще-то...") — это нормальный рабочий стиль сессии, не повод считать, что план сорвался; переспрашивать до полной ясности, а не додумывать за него.

## Краткая история версий (для ориентира, не требует пересказа пользователю)

- **v1.5.10** — недельный отчёт: добавлена хронология кормлений с датами; фикс сброса горизонтального скролла стеллажа при массовом кормлении.
- **v1.6.0** — состояние КО (живой/предубит/разморозка) и логика утилизации; массовая утренняя проверка; `timestamp` больше не перезаписывается при разрешении "на ночь", добавлен `resolvedAt`.
- **v1.7.0** — новый отчёт за календарную неделю (понедельник–воскресенье) с навигацией по прошлым неделям и блоком "Итого расход КО" по видам/размерам; переименование колонки "Утилизация" → "Расход КО"; старый отчёт за скользящую неделю не изменён по поведению, только рефакторен на общие функции.
- **v1.8.0** — новая функция "Инкубация": блок-стеллаж типа "Инкубатор" (ячейки только под кладки), карточка кладки (дата, кол-во яиц, статус, привязка родителя), отдельный журнал температуры+влажности у кладки, архив кладок, отчёт по инкубации. Бэкап/импорт расширены на архив кладок.
- **v1.8.1** — фикс: нижняя навигация обрезала пункты (в т.ч. "Бэкап") на узких портретных экранах из-за добавленного пункта "Инкубация" — теперь скроллится вбок при нехватке места.
- **v1.8.2** — целевой температурный режим блока-инкубатора (журнал с гистерезисом min/max, не разовое поле); визуальное выделение инкубатора бирюзовым (`--incubator`); у кладки — дата+время закладки (было только дата), вид животного с автоподсказкой, срок инкубации в сутках; ежедневные всплывающие напоминания о приближающемся/просроченном сроке вылупления.
- **v1.8.3** — массовая отметка "Не съедено" в утренней проверке (зеркальная к уже существующей "Съедено полностью"), только для предубитого/размороженного КО — живой КО в неё сознательно не входит.
- **v1.8.4** — массовая обработка живого КО в утренней проверке: переключатель "Жив"/"Погиб" у каждой такой записи прямо в списке (по умолчанию "Жив") + кнопка "Применить выбранное", читающая состояние каждого переключателя. Закрывает сценарий "N ячеек с живым КО на ночь, часть погибла" за пару действий вместо N.
- **v1.8.5** — все поля заметок/причин/комментариев переведены в многострочные `textarea` с единым тёмным стилем (раньше `<textarea>` не была включена в общее CSS-правило стилизации полей); добавлено `escapeHtml()` для корректного рендера спецсимволов в инлайн-редактировании комментария лога.
- **v1.8.6** — фоновая температура локации целиком (новый журнал `loc.ambientTempLog`, кнопка в шапке локации); общий отчёт по температуре (ячейки + фоновая локации вместе) в «Статистика и отчёты»; формат температурных записей `дата: значение` вместо `дата — значение` (все 4 температурных журнала); уточнена формулировка в модалке показания ячейки, чтобы не путать с фоновой температурой локации.
- **v1.8.7** — отмена массовых действий: 5-минутное окно отмены (с подсветкой затронутых записей) для массового кормления и всех 3 кнопок массовой утренней проверки, переживает закрытие/перезапуск приложения; точечная правка одной записи выводит её из-под отката, остальной пакет остаётся активным; несколько массовых действий подряд — независимые параллельные пакеты. Индивидуальная форма правки записи кормления (✏) научилась менять состояние КО.
- **v1.8.8** — исправлен баг переезда животного: форма трансфера по умолчанию открывалась на первой локации в списке, а не на текущей локации животного (причина реального инцидента — животное "уехало" не туда); заодно закрыта отдельно найденная опасность необратимой потери животного при недоступной целевой ячейке (изменён порядок операций — сначала проверка цели, потом удаление из источника); занятые ячейки в списке трансфера теперь видны с пометкой вместо скрытия; добавлена живая строка "Из/В" и понятное сообщение при отсутствии доступных ячеек.
- **v1.8.9** — частичная гибель КО при групповой подаче: в индивидуальной карточке животного бинарный переключатель "Жив/Погиб" заменён на числовое поле "Погибло, шт" (плюс живой пересчёт "Осталось живо"), видно при любом непойманном остатке, а не только при полном отказе. Массовая утренняя проверка сознательно не детализирована — там остался прежний бинарный переключатель. Расход КО в отчётах считается точнее ("съедено + погибло"), старые записи мигрируют без потери точности статистики.
- **v1.8.10** — окно отмены (5 минут, `pendingActions`) для критических индивидуальных действий: удаление ячейки/стеллажа/локации, архивация животного/кладки, переезд животного. Отмена животного/кладки/ячейки/переезда — бейдж в шапке стеллажа (как у массовых действий); удаление стеллажа/локации целиком — отдельная глобальная плашка наверху экрана, т.к. у них нет живого "родителя" для якоря. Тот же принятый риск позиционной адресации при структурных правках в момент отката, что уже был описан для переезда животного в v1.8.8.
- **v1.8.11** — отчёт "Карта расселения": визуальная карта ячеек по стеллажам/рядам (вид+пол прямо на карточке ячейки) + список животных под ней (Стеллаж→Ячейка→Вид→Пол→Возраст). Два входа: по одной локации (в "Статистика и отчёты") и по всей коллекции сразу (кнопка в нижней навигации, со сводной таблицей и разрывом страницы между локациями при печати). Ячейки ряда переносятся по ширине страницы вместо таблицы с фиксированными столбцами — длинные ряды (20+ ячеек подряд) не обрезаются при печати. Блоки-инкубаторы исключены, служебные ячейки помечены отдельно.
- **v1.8.12** — доработка вида «Карты расселения» по отзыву пользователя: плитки ячеек переделаны под стиль самого приложения (компактная фиксированная ширина, синяя рамка у занятых, штриховка у служебных — вместо текстовых карточек из v1.8.11), но **без обрезания текста** (в отличие от настоящих `.cell` на экране, где текст обрезается до 2 строк) — высота плитки растёт под содержимое. Светлый фон в печатном/скачиваемом файле, тёмный — в превью внутри приложения.
- **v1.8.13** — исправлено визуальное поведение карты расселения: пустые ячейки "ужимались" по высоте рядом с многострочными занятыми соседями по той же визуальной строке ряда (`align-items: flex-start`) — заменено на `align-items: stretch` в обоих местах (тёмное превью + светлая печать), теперь все плитки одной строки ряда равны по высоте самой высокой из них.
- **v1.8.14** — два новых отчёта для планирования закупок: «Потребность в КО (за период)» и «Типичная порция (последние 4 кормления)», оба за 28 дней назад от выбранной пользователем даты / без привязки к датам соответственно. Первая версия имела разбивку по видам и локациям — упрощена в v1.8.15, см. ниже.
- **v1.8.15** — упрощение отчётов по прямой правке пользователя: «Потребность в КО» лишена разбивки по видам животных и по локациям (в варианте "вся коллекция") — теперь одна таблица тип+размер КО, без формулировок вроде "для закупки". «Типичная порция» переименована в **«Статистика поедаемости»** (разбивка по видам там осталась — уместна по смыслу отчёта).

## Отложенные направления (НЕ начинать без явного запроса и отдельного обсуждения)

Обсуждались, но не реализуются в текущей архитектуре без отдельного решения пользователя:

- **Аккаунты и права доступа.** Сейчас в приложении нет понятия пользователя вообще — только общий PIN на инженерный режим, один на всех. Подробно спроектировано (см. `SYNC_PLAN.md` и историю обсуждения) в связке с синхронизацией: отдельный **инженерный пароль** (не PIN) — хэш `SHA-256` зашит в код константой, проверяется через `crypto.subtle.digest`, сознательно отделён от токена синхронизации (утечка одного не даёт доступа к другому); список **разрешённых локаций** настраивается на устройстве (пустой список = без ограничений — так устроено устройство админа), чужие локации видны, но только на чтение, с пометкой "🔒 Только просмотр"; сброс PIN на устройстве с настроенной синхронизацией требует ввода токена синхронизации, а не работает как самообслуживание. Ничего из этого ещё не реализовано в коде — see `SYNC_PLAN.md`, часть D, этапы 4 и 6.
- **Синхронизация между устройствами.** Обсуждён широкий спектр вариантов (Firebase/Firestore, Supabase, CouchDB/PouchDB, P2P/CRDT, облачный диск, GitHub-as-storage, готовый облачный KV-сервис, Google Sheets, WebDAV, Nextcloud, Syncthing, Node.js+WebSocket, а также полноценный собственный бэкенд с БД — последний явно отклонён после сверки с реальными ресурсами пользователя, см. историю обсуждения). **Финальное зафиксированное направление:** Google-таблица + Apps Script (не PHP — более ранний вариант в этом файле устарел). Полный план — структура таблицы, код `Code.gs`, пошаговые инструкции по GitHub/таблице/Apps Script, порядок этапов (1–6, без QR — исключён как избыточный) — в отдельном файле **`SYNC_PLAN.md`**, актуализировать вместе с ним при каждой сессии по этой теме, как и `CLAUDE.md`. Ключевая идея защиты от конфликтов при отправке: "сначала свежая версия с сервера → подменить только СВОИ (реально изменённые, отслеживаются флажком) локации → отправить" — не одна большая перезапись файла целиком. Требует переноса в новый репозиторий первым шагом (смена префикса `localStorage`-ключей с `exofeed_` на `exofeed2_`, см. `SYNC_PLAN.md` часть A) — не начинать реализацию без явного запроса и подтверждённого плана.
- **Устойчивые (постоянные) ID для рядов/ячеек/животных/кладок.** Сейчас постоянный `id` (генерируется через `Date.now()` при создании, никогда не меняется) есть только у локации и стеллажа — у рядов, ячеек, животных и кладок адрес вычисляется каждый раз заново по позиции в массиве (см. "Адрес ячейки... не хранится" выше). Это создаёт разделяемый риск в нескольких местах: переезд животного (v1.8.8) и откат критических действий (`pendingActions`, v1.8.10) могут в редких случаях (структурная правка того же стеллажа в течение открытого 5-минутного окна) вернуть объект не строго на прежнюю позицию, а по соседству — данные при этом не теряются, но точность позиции не гарантирована. Полное решение — дать устойчивый номер и этим сущностям тоже, но это **не локальная правка**: затронет drag-and-drop, массовое кормление/проверку, историю кормления, отчёты — везде, где сейчас адресация идёт по индексу в массиве. Отдельная, самостоятельная сессия планирования, не довесок к другой задаче (обсуждалось и осознанно отложено при обсуждении v1.8.10).
- **Англоязычный форк** — статический перевод текущего приложения без слоя переключения языков, отдельный репозиторий, независимая нумерация версий.

Если пользователь просит продолжить любое из этих направлений — сначала свериться с ним, что план не изменился с момента последнего обсуждения (мог устареть), прежде чем писать код.
